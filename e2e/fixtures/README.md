`speech.wav` contains an original test sentence synthesized locally with macOS Samantha, converted to 16 kHz mono PCM:

> Welcome to the podcast. We are checking voice activity detection. Only spoken words should be transcribed.

The VAD browser test surrounds this speech with four seconds of silence on each side. This fixture requires no external downloads, microphones, or user recordings.
