# Later programs (not tickets yet)

Do not implement in the current marker/export/search program.

- **Marker filter / hidden:** list filter by type; filtered marks stay knowable but quiet. Selection (Cmd+A) only hits visible marks.
- **Save history:** harder to clobber earlier work (versioned saves). Not the current “refuse overwrite unless bound” rule.
